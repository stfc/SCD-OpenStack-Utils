basic_auth_users:
    {{ prometheus_username }}: {{ prometheus_password | ansible.builtin.password_hash(hashtype="bcrypt") }}

tls_server_config:
  cert_file: /opt/prometheus/prometheus.crt
  key_file: /opt/prometheus/prometheus.key
