basic_auth_users:
    {{ alertmanager_username }}: {{ alertmanager_password | ansible.builtin.password_hash(hashtype="bcrypt") }}

tls_server_config:
  cert_file: /opt/alertmanager/alertmanager.crt
  key_file: /opt/alertmanager/alertmanager.key